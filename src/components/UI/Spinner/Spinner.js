import React from 'react'
import clsses from './Spinner.module.css'

export const Spinner = () => {
    return (
            <div className={clsses.loader}>Loading...</div>
    )
}

export default Spinner